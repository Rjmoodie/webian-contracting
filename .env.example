# Optional: override Supabase project (default is Webian project sdxhppnsfgvoqcyxcfai)
# Get these from: https://supabase.com/dashboard/project/sdxhppnsfgvoqcyxcfai/settings/api
# VITE_SUPABASE_URL=https://sdxhppnsfgvoqcyxcfai.supabase.co
# VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Client / branding (optional; default is webiancontracting)
# VITE_CLIENT_ID=webiancontracting

# Google Maps Places API key (for address autocomplete in RFQ form)
# Get one at: https://console.cloud.google.com/apis/credentials
# Enable "Places API" and "Maps JavaScript API" on the key
# VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# -----------------------------------------------------------------------------
# Supabase Edge Function secrets (set in Dashboard, not in .env)
# Dashboard: Project → Edge Functions → make-server-d8ea749c → Settings → Secrets
# -----------------------------------------------------------------------------
# Restrict who can complete admin signup (Admin signup page).
# Comma-separated list of allowed emails; only these can sign up as admin.
# Leave unset to allow any email to use the Admin signup page.
# Example: ALLOWED_ADMIN_EMAILS=rodzrj@gmail.com,admin@yourcompany.com
